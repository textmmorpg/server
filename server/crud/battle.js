const db = require('./db').get_db();

module.exports = {
    death
}

async function death(socket) {
    // await db.collection('user').updateOne({
    //     socket_id: socket.id
    // }, {

    // });
}